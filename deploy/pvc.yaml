apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elapsed-local-path-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Mi
